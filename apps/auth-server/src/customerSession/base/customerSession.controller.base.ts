/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CustomerSessionService } from "../customerSession.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CustomerSessionCreateInput } from "./CustomerSessionCreateInput";
import { CustomerSession } from "./CustomerSession";
import { CustomerSessionFindManyArgs } from "./CustomerSessionFindManyArgs";
import { CustomerSessionWhereUniqueInput } from "./CustomerSessionWhereUniqueInput";
import { CustomerSessionUpdateInput } from "./CustomerSessionUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CustomerSessionControllerBase {
  constructor(
    protected readonly service: CustomerSessionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CustomerSession })
  @swagger.ApiBody({
    type: CustomerSessionCreateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "CustomerSession",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createCustomerSession(
    @common.Body() data: CustomerSessionCreateInput
  ): Promise<CustomerSession> {
    return await this.service.createCustomerSession({
      data: {
        ...data,

        customer: {
          connect: data.customer,
        },
      },
      select: {
        bytesIn: true,
        bytesOut: true,

        customer: {
          select: {
            id: true,
          },
        },

        endedAt: true,
        id: true,
        ipAddress: true,
        lastSeenAt: true,
        macAddress: true,
        sessionId: true,
        startedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CustomerSession] })
  @ApiNestedQuery(CustomerSessionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CustomerSession",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async customerSessions(
    @common.Req() request: Request
  ): Promise<CustomerSession[]> {
    const args = plainToClass(CustomerSessionFindManyArgs, request.query);
    return this.service.customerSessions({
      ...args,
      select: {
        bytesIn: true,
        bytesOut: true,

        customer: {
          select: {
            id: true,
          },
        },

        endedAt: true,
        id: true,
        ipAddress: true,
        lastSeenAt: true,
        macAddress: true,
        sessionId: true,
        startedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CustomerSession })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CustomerSession",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async customerSession(
    @common.Param() params: CustomerSessionWhereUniqueInput
  ): Promise<CustomerSession | null> {
    const result = await this.service.customerSession({
      where: params,
      select: {
        bytesIn: true,
        bytesOut: true,

        customer: {
          select: {
            id: true,
          },
        },

        endedAt: true,
        id: true,
        ipAddress: true,
        lastSeenAt: true,
        macAddress: true,
        sessionId: true,
        startedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CustomerSession })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiBody({
    type: CustomerSessionUpdateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "CustomerSession",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateCustomerSession(
    @common.Param() params: CustomerSessionWhereUniqueInput,
    @common.Body() data: CustomerSessionUpdateInput
  ): Promise<CustomerSession | null> {
    try {
      return await this.service.updateCustomerSession({
        where: params,
        data: {
          ...data,

          customer: {
            connect: data.customer,
          },
        },
        select: {
          bytesIn: true,
          bytesOut: true,

          customer: {
            select: {
              id: true,
            },
          },

          endedAt: true,
          id: true,
          ipAddress: true,
          lastSeenAt: true,
          macAddress: true,
          sessionId: true,
          startedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CustomerSession })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CustomerSession",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCustomerSession(
    @common.Param() params: CustomerSessionWhereUniqueInput
  ): Promise<CustomerSession | null> {
    try {
      return await this.service.deleteCustomerSession({
        where: params,
        select: {
          bytesIn: true,
          bytesOut: true,

          customer: {
            select: {
              id: true,
            },
          },

          endedAt: true,
          id: true,
          ipAddress: true,
          lastSeenAt: true,
          macAddress: true,
          sessionId: true,
          startedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
