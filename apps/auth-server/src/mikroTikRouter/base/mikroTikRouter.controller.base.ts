/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { MikroTikRouterService } from "../mikroTikRouter.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { MikroTikRouterCreateInput } from "./MikroTikRouterCreateInput";
import { MikroTikRouter } from "./MikroTikRouter";
import { MikroTikRouterFindManyArgs } from "./MikroTikRouterFindManyArgs";
import { MikroTikRouterWhereUniqueInput } from "./MikroTikRouterWhereUniqueInput";
import { MikroTikRouterUpdateInput } from "./MikroTikRouterUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class MikroTikRouterControllerBase {
  constructor(
    protected readonly service: MikroTikRouterService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: MikroTikRouter })
  @swagger.ApiBody({
    type: MikroTikRouterCreateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createMikroTikRouter(
    @common.Body() data: MikroTikRouterCreateInput
  ): Promise<MikroTikRouter> {
    return await this.service.createMikroTikRouter({
      data: {
        ...data,

        pop: data.pop
          ? {
              connect: data.pop,
            }
          : undefined,
      },
      select: {
        apiPassword: true,
        apiPort: true,
        apiUser: true,
        createdAt: true,
        host: true,
        id: true,
        isActive: true,
        name: true,

        pop: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [MikroTikRouter] })
  @ApiNestedQuery(MikroTikRouterFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async mikroTikRouters(
    @common.Req() request: Request
  ): Promise<MikroTikRouter[]> {
    const args = plainToClass(MikroTikRouterFindManyArgs, request.query);
    return this.service.mikroTikRouters({
      ...args,
      select: {
        apiPassword: true,
        apiPort: true,
        apiUser: true,
        createdAt: true,
        host: true,
        id: true,
        isActive: true,
        name: true,

        pop: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: MikroTikRouter })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async mikroTikRouter(
    @common.Param() params: MikroTikRouterWhereUniqueInput
  ): Promise<MikroTikRouter | null> {
    const result = await this.service.mikroTikRouter({
      where: params,
      select: {
        apiPassword: true,
        apiPort: true,
        apiUser: true,
        createdAt: true,
        host: true,
        id: true,
        isActive: true,
        name: true,

        pop: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: MikroTikRouter })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiBody({
    type: MikroTikRouterUpdateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateMikroTikRouter(
    @common.Param() params: MikroTikRouterWhereUniqueInput,
    @common.Body() data: MikroTikRouterUpdateInput
  ): Promise<MikroTikRouter | null> {
    try {
      return await this.service.updateMikroTikRouter({
        where: params,
        data: {
          ...data,

          pop: data.pop
            ? {
                connect: data.pop,
              }
            : undefined,
        },
        select: {
          apiPassword: true,
          apiPort: true,
          apiUser: true,
          createdAt: true,
          host: true,
          id: true,
          isActive: true,
          name: true,

          pop: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: MikroTikRouter })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteMikroTikRouter(
    @common.Param() params: MikroTikRouterWhereUniqueInput
  ): Promise<MikroTikRouter | null> {
    try {
      return await this.service.deleteMikroTikRouter({
        where: params,
        select: {
          apiPassword: true,
          apiPort: true,
          apiUser: true,
          createdAt: true,
          host: true,
          id: true,
          isActive: true,
          name: true,

          pop: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
