/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { MikroTikRouter } from "./MikroTikRouter";
import { MikroTikRouterCountArgs } from "./MikroTikRouterCountArgs";
import { MikroTikRouterFindManyArgs } from "./MikroTikRouterFindManyArgs";
import { MikroTikRouterFindUniqueArgs } from "./MikroTikRouterFindUniqueArgs";
import { CreateMikroTikRouterArgs } from "./CreateMikroTikRouterArgs";
import { UpdateMikroTikRouterArgs } from "./UpdateMikroTikRouterArgs";
import { DeleteMikroTikRouterArgs } from "./DeleteMikroTikRouterArgs";
import { Pop } from "../../pop/base/Pop";
import { MikroTikRouterService } from "../mikroTikRouter.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => MikroTikRouter)
export class MikroTikRouterResolverBase {
  constructor(
    protected readonly service: MikroTikRouterService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "read",
    possession: "any",
  })
  async _mikroTikRoutersMeta(
    @graphql.Args() args: MikroTikRouterCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [MikroTikRouter])
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "read",
    possession: "any",
  })
  async mikroTikRouters(
    @graphql.Args() args: MikroTikRouterFindManyArgs
  ): Promise<MikroTikRouter[]> {
    return this.service.mikroTikRouters(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => MikroTikRouter, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "read",
    possession: "own",
  })
  async mikroTikRouter(
    @graphql.Args() args: MikroTikRouterFindUniqueArgs
  ): Promise<MikroTikRouter | null> {
    const result = await this.service.mikroTikRouter(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => MikroTikRouter)
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "create",
    possession: "any",
  })
  async createMikroTikRouter(
    @graphql.Args() args: CreateMikroTikRouterArgs
  ): Promise<MikroTikRouter> {
    return await this.service.createMikroTikRouter({
      ...args,
      data: {
        ...args.data,

        pop: args.data.pop
          ? {
              connect: args.data.pop,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => MikroTikRouter)
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "update",
    possession: "any",
  })
  async updateMikroTikRouter(
    @graphql.Args() args: UpdateMikroTikRouterArgs
  ): Promise<MikroTikRouter | null> {
    try {
      return await this.service.updateMikroTikRouter({
        ...args,
        data: {
          ...args.data,

          pop: args.data.pop
            ? {
                connect: args.data.pop,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => MikroTikRouter)
  @nestAccessControl.UseRoles({
    resource: "MikroTikRouter",
    action: "delete",
    possession: "any",
  })
  async deleteMikroTikRouter(
    @graphql.Args() args: DeleteMikroTikRouterArgs
  ): Promise<MikroTikRouter | null> {
    try {
      return await this.service.deleteMikroTikRouter(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Pop, {
    nullable: true,
    name: "pop",
  })
  @nestAccessControl.UseRoles({
    resource: "Pop",
    action: "read",
    possession: "any",
  })
  async getPop(@graphql.Parent() parent: MikroTikRouter): Promise<Pop | null> {
    const result = await this.service.getPop(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
