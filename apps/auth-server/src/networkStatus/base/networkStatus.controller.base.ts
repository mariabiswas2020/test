/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { NetworkStatusService } from "../networkStatus.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { NetworkStatusCreateInput } from "./NetworkStatusCreateInput";
import { NetworkStatus } from "./NetworkStatus";
import { NetworkStatusFindManyArgs } from "./NetworkStatusFindManyArgs";
import { NetworkStatusWhereUniqueInput } from "./NetworkStatusWhereUniqueInput";
import { NetworkStatusUpdateInput } from "./NetworkStatusUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class NetworkStatusControllerBase {
  constructor(
    protected readonly service: NetworkStatusService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: NetworkStatus })
  @swagger.ApiBody({
    type: NetworkStatusCreateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createNetworkStatus(
    @common.Body() data: NetworkStatusCreateInput
  ): Promise<NetworkStatus> {
    return await this.service.createNetworkStatus({
      data: data,
      select: {
        createdAt: true,
        id: true,
        latency: true,
        region: true,
        timestamp: true,
        uptime: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [NetworkStatus] })
  @ApiNestedQuery(NetworkStatusFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async networkStatuses(
    @common.Req() request: Request
  ): Promise<NetworkStatus[]> {
    const args = plainToClass(NetworkStatusFindManyArgs, request.query);
    return this.service.networkStatuses({
      ...args,
      select: {
        createdAt: true,
        id: true,
        latency: true,
        region: true,
        timestamp: true,
        uptime: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: NetworkStatus })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async networkStatus(
    @common.Param() params: NetworkStatusWhereUniqueInput
  ): Promise<NetworkStatus | null> {
    const result = await this.service.networkStatus({
      where: params,
      select: {
        createdAt: true,
        id: true,
        latency: true,
        region: true,
        timestamp: true,
        uptime: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: NetworkStatus })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiBody({
    type: NetworkStatusUpdateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateNetworkStatus(
    @common.Param() params: NetworkStatusWhereUniqueInput,
    @common.Body() data: NetworkStatusUpdateInput
  ): Promise<NetworkStatus | null> {
    try {
      return await this.service.updateNetworkStatus({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          latency: true,
          region: true,
          timestamp: true,
          uptime: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: NetworkStatus })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteNetworkStatus(
    @common.Param() params: NetworkStatusWhereUniqueInput
  ): Promise<NetworkStatus | null> {
    try {
      return await this.service.deleteNetworkStatus({
        where: params,
        select: {
          createdAt: true,
          id: true,
          latency: true,
          region: true,
          timestamp: true,
          uptime: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
