/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { NetworkStatus } from "./NetworkStatus";
import { NetworkStatusCountArgs } from "./NetworkStatusCountArgs";
import { NetworkStatusFindManyArgs } from "./NetworkStatusFindManyArgs";
import { NetworkStatusFindUniqueArgs } from "./NetworkStatusFindUniqueArgs";
import { CreateNetworkStatusArgs } from "./CreateNetworkStatusArgs";
import { UpdateNetworkStatusArgs } from "./UpdateNetworkStatusArgs";
import { DeleteNetworkStatusArgs } from "./DeleteNetworkStatusArgs";
import { NetworkStatusService } from "../networkStatus.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => NetworkStatus)
export class NetworkStatusResolverBase {
  constructor(
    protected readonly service: NetworkStatusService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "read",
    possession: "any",
  })
  async _networkStatusesMeta(
    @graphql.Args() args: NetworkStatusCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [NetworkStatus])
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "read",
    possession: "any",
  })
  async networkStatuses(
    @graphql.Args() args: NetworkStatusFindManyArgs
  ): Promise<NetworkStatus[]> {
    return this.service.networkStatuses(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => NetworkStatus, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "read",
    possession: "own",
  })
  async networkStatus(
    @graphql.Args() args: NetworkStatusFindUniqueArgs
  ): Promise<NetworkStatus | null> {
    const result = await this.service.networkStatus(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => NetworkStatus)
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "create",
    possession: "any",
  })
  async createNetworkStatus(
    @graphql.Args() args: CreateNetworkStatusArgs
  ): Promise<NetworkStatus> {
    return await this.service.createNetworkStatus({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => NetworkStatus)
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "update",
    possession: "any",
  })
  async updateNetworkStatus(
    @graphql.Args() args: UpdateNetworkStatusArgs
  ): Promise<NetworkStatus | null> {
    try {
      return await this.service.updateNetworkStatus({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => NetworkStatus)
  @nestAccessControl.UseRoles({
    resource: "NetworkStatus",
    action: "delete",
    possession: "any",
  })
  async deleteNetworkStatus(
    @graphql.Args() args: DeleteNetworkStatusArgs
  ): Promise<NetworkStatus | null> {
    try {
      return await this.service.deleteNetworkStatus(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
