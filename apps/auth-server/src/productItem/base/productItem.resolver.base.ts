/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { ProductItem } from "./ProductItem";
import { ProductItemCountArgs } from "./ProductItemCountArgs";
import { ProductItemFindManyArgs } from "./ProductItemFindManyArgs";
import { ProductItemFindUniqueArgs } from "./ProductItemFindUniqueArgs";
import { CreateProductItemArgs } from "./CreateProductItemArgs";
import { UpdateProductItemArgs } from "./UpdateProductItemArgs";
import { DeleteProductItemArgs } from "./DeleteProductItemArgs";
import { Pop } from "../../pop/base/Pop";
import { Product } from "../../product/base/Product";
import { Purchase } from "../../purchase/base/Purchase";
import { ProductItemService } from "../productItem.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ProductItem)
export class ProductItemResolverBase {
  constructor(
    protected readonly service: ProductItemService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ProductItem",
    action: "read",
    possession: "any",
  })
  async _productItemsMeta(
    @graphql.Args() args: ProductItemCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ProductItem])
  @nestAccessControl.UseRoles({
    resource: "ProductItem",
    action: "read",
    possession: "any",
  })
  async productItems(
    @graphql.Args() args: ProductItemFindManyArgs
  ): Promise<ProductItem[]> {
    return this.service.productItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ProductItem, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ProductItem",
    action: "read",
    possession: "own",
  })
  async productItem(
    @graphql.Args() args: ProductItemFindUniqueArgs
  ): Promise<ProductItem | null> {
    const result = await this.service.productItem(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ProductItem)
  @nestAccessControl.UseRoles({
    resource: "ProductItem",
    action: "create",
    possession: "any",
  })
  async createProductItem(
    @graphql.Args() args: CreateProductItemArgs
  ): Promise<ProductItem> {
    return await this.service.createProductItem({
      ...args,
      data: {
        ...args.data,

        locationPop: args.data.locationPop
          ? {
              connect: args.data.locationPop,
            }
          : undefined,

        product: {
          connect: args.data.product,
        },

        purchase: args.data.purchase
          ? {
              connect: args.data.purchase,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ProductItem)
  @nestAccessControl.UseRoles({
    resource: "ProductItem",
    action: "update",
    possession: "any",
  })
  async updateProductItem(
    @graphql.Args() args: UpdateProductItemArgs
  ): Promise<ProductItem | null> {
    try {
      return await this.service.updateProductItem({
        ...args,
        data: {
          ...args.data,

          locationPop: args.data.locationPop
            ? {
                connect: args.data.locationPop,
              }
            : undefined,

          product: {
            connect: args.data.product,
          },

          purchase: args.data.purchase
            ? {
                connect: args.data.purchase,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => ProductItem)
  @nestAccessControl.UseRoles({
    resource: "ProductItem",
    action: "delete",
    possession: "any",
  })
  async deleteProductItem(
    @graphql.Args() args: DeleteProductItemArgs
  ): Promise<ProductItem | null> {
    try {
      return await this.service.deleteProductItem(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Pop, {
    nullable: true,
    name: "locationPop",
  })
  @nestAccessControl.UseRoles({
    resource: "Pop",
    action: "read",
    possession: "any",
  })
  async getLocationPop(
    @graphql.Parent() parent: ProductItem
  ): Promise<Pop | null> {
    const result = await this.service.getLocationPop(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Product, {
    nullable: true,
    name: "product",
  })
  @nestAccessControl.UseRoles({
    resource: "Product",
    action: "read",
    possession: "any",
  })
  async getProduct(
    @graphql.Parent() parent: ProductItem
  ): Promise<Product | null> {
    const result = await this.service.getProduct(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Purchase, {
    nullable: true,
    name: "purchase",
  })
  @nestAccessControl.UseRoles({
    resource: "Purchase",
    action: "read",
    possession: "any",
  })
  async getPurchase(
    @graphql.Parent() parent: ProductItem
  ): Promise<Purchase | null> {
    const result = await this.service.getPurchase(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
