/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { ResellerPackage } from "./ResellerPackage";
import { ResellerPackageCountArgs } from "./ResellerPackageCountArgs";
import { ResellerPackageFindManyArgs } from "./ResellerPackageFindManyArgs";
import { ResellerPackageFindUniqueArgs } from "./ResellerPackageFindUniqueArgs";
import { CreateResellerPackageArgs } from "./CreateResellerPackageArgs";
import { UpdateResellerPackageArgs } from "./UpdateResellerPackageArgs";
import { DeleteResellerPackageArgs } from "./DeleteResellerPackageArgs";
import { PackageModel } from "../../packageModel/base/PackageModel";
import { Reseller } from "../../reseller/base/Reseller";
import { ResellerPackageService } from "../resellerPackage.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ResellerPackage)
export class ResellerPackageResolverBase {
  constructor(
    protected readonly service: ResellerPackageService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ResellerPackage",
    action: "read",
    possession: "any",
  })
  async _resellerPackagesMeta(
    @graphql.Args() args: ResellerPackageCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ResellerPackage])
  @nestAccessControl.UseRoles({
    resource: "ResellerPackage",
    action: "read",
    possession: "any",
  })
  async resellerPackages(
    @graphql.Args() args: ResellerPackageFindManyArgs
  ): Promise<ResellerPackage[]> {
    return this.service.resellerPackages(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ResellerPackage, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ResellerPackage",
    action: "read",
    possession: "own",
  })
  async resellerPackage(
    @graphql.Args() args: ResellerPackageFindUniqueArgs
  ): Promise<ResellerPackage | null> {
    const result = await this.service.resellerPackage(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ResellerPackage)
  @nestAccessControl.UseRoles({
    resource: "ResellerPackage",
    action: "create",
    possession: "any",
  })
  async createResellerPackage(
    @graphql.Args() args: CreateResellerPackageArgs
  ): Promise<ResellerPackage> {
    return await this.service.createResellerPackage({
      ...args,
      data: {
        ...args.data,

        packageField: {
          connect: args.data.packageField,
        },

        reseller: {
          connect: args.data.reseller,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ResellerPackage)
  @nestAccessControl.UseRoles({
    resource: "ResellerPackage",
    action: "update",
    possession: "any",
  })
  async updateResellerPackage(
    @graphql.Args() args: UpdateResellerPackageArgs
  ): Promise<ResellerPackage | null> {
    try {
      return await this.service.updateResellerPackage({
        ...args,
        data: {
          ...args.data,

          packageField: {
            connect: args.data.packageField,
          },

          reseller: {
            connect: args.data.reseller,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => ResellerPackage)
  @nestAccessControl.UseRoles({
    resource: "ResellerPackage",
    action: "delete",
    possession: "any",
  })
  async deleteResellerPackage(
    @graphql.Args() args: DeleteResellerPackageArgs
  ): Promise<ResellerPackage | null> {
    try {
      return await this.service.deleteResellerPackage(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => PackageModel, {
    nullable: true,
    name: "packageField",
  })
  @nestAccessControl.UseRoles({
    resource: "PackageModel",
    action: "read",
    possession: "any",
  })
  async getPackageField(
    @graphql.Parent() parent: ResellerPackage
  ): Promise<PackageModel | null> {
    const result = await this.service.getPackageField(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Reseller, {
    nullable: true,
    name: "reseller",
  })
  @nestAccessControl.UseRoles({
    resource: "Reseller",
    action: "read",
    possession: "any",
  })
  async getReseller(
    @graphql.Parent() parent: ResellerPackage
  ): Promise<Reseller | null> {
    const result = await this.service.getReseller(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
