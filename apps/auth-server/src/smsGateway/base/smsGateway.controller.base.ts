/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { SmsGatewayService } from "../smsGateway.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SmsGatewayCreateInput } from "./SmsGatewayCreateInput";
import { SmsGateway } from "./SmsGateway";
import { SmsGatewayFindManyArgs } from "./SmsGatewayFindManyArgs";
import { SmsGatewayWhereUniqueInput } from "./SmsGatewayWhereUniqueInput";
import { SmsGatewayUpdateInput } from "./SmsGatewayUpdateInput";
import { SmsLogFindManyArgs } from "../../smsLog/base/SmsLogFindManyArgs";
import { SmsLog } from "../../smsLog/base/SmsLog";
import { SmsLogWhereUniqueInput } from "../../smsLog/base/SmsLogWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SmsGatewayControllerBase {
  constructor(
    protected readonly service: SmsGatewayService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SmsGateway })
  @swagger.ApiBody({
    type: SmsGatewayCreateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createSmsGateway(
    @common.Body() data: SmsGatewayCreateInput
  ): Promise<SmsGateway> {
    return await this.service.createSmsGateway({
      data: data,
      select: {
        apiKey: true,
        apiSecret: true,
        apiUrl: true,
        createdAt: true,
        id: true,
        isActive: true,
        isDefault: true,
        name: true,
        provider: true,
        senderId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [SmsGateway] })
  @ApiNestedQuery(SmsGatewayFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async smsGateways(@common.Req() request: Request): Promise<SmsGateway[]> {
    const args = plainToClass(SmsGatewayFindManyArgs, request.query);
    return this.service.smsGateways({
      ...args,
      select: {
        apiKey: true,
        apiSecret: true,
        apiUrl: true,
        createdAt: true,
        id: true,
        isActive: true,
        isDefault: true,
        name: true,
        provider: true,
        senderId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SmsGateway })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async smsGateway(
    @common.Param() params: SmsGatewayWhereUniqueInput
  ): Promise<SmsGateway | null> {
    const result = await this.service.smsGateway({
      where: params,
      select: {
        apiKey: true,
        apiSecret: true,
        apiUrl: true,
        createdAt: true,
        id: true,
        isActive: true,
        isDefault: true,
        name: true,
        provider: true,
        senderId: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SmsGateway })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiBody({
    type: SmsGatewayUpdateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateSmsGateway(
    @common.Param() params: SmsGatewayWhereUniqueInput,
    @common.Body() data: SmsGatewayUpdateInput
  ): Promise<SmsGateway | null> {
    try {
      return await this.service.updateSmsGateway({
        where: params,
        data: data,
        select: {
          apiKey: true,
          apiSecret: true,
          apiUrl: true,
          createdAt: true,
          id: true,
          isActive: true,
          isDefault: true,
          name: true,
          provider: true,
          senderId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SmsGateway })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteSmsGateway(
    @common.Param() params: SmsGatewayWhereUniqueInput
  ): Promise<SmsGateway | null> {
    try {
      return await this.service.deleteSmsGateway({
        where: params,
        select: {
          apiKey: true,
          apiSecret: true,
          apiUrl: true,
          createdAt: true,
          id: true,
          isActive: true,
          isDefault: true,
          name: true,
          provider: true,
          senderId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/logs")
  @ApiNestedQuery(SmsLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SmsLog",
    action: "read",
    possession: "any",
  })
  async findLogs(
    @common.Req() request: Request,
    @common.Param() params: SmsGatewayWhereUniqueInput
  ): Promise<SmsLog[]> {
    const query = plainToClass(SmsLogFindManyArgs, request.query);
    const results = await this.service.findLogs(params.id, {
      ...query,
      select: {
        createdAt: true,
        errorMsg: true,

        gateway: {
          select: {
            id: true,
          },
        },

        id: true,
        message: true,
        phone: true,
        sentAt: true,
        status: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/logs")
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "update",
    possession: "any",
  })
  async connectLogs(
    @common.Param() params: SmsGatewayWhereUniqueInput,
    @common.Body() body: SmsLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      logs: {
        connect: body,
      },
    };
    await this.service.updateSmsGateway({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/logs")
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "update",
    possession: "any",
  })
  async updateLogs(
    @common.Param() params: SmsGatewayWhereUniqueInput,
    @common.Body() body: SmsLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      logs: {
        set: body,
      },
    };
    await this.service.updateSmsGateway({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/logs")
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "update",
    possession: "any",
  })
  async disconnectLogs(
    @common.Param() params: SmsGatewayWhereUniqueInput,
    @common.Body() body: SmsLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      logs: {
        disconnect: body,
      },
    };
    await this.service.updateSmsGateway({
      where: params,
      data,
      select: { id: true },
    });
  }
}
