/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { SmsGateway } from "./SmsGateway";
import { SmsGatewayCountArgs } from "./SmsGatewayCountArgs";
import { SmsGatewayFindManyArgs } from "./SmsGatewayFindManyArgs";
import { SmsGatewayFindUniqueArgs } from "./SmsGatewayFindUniqueArgs";
import { CreateSmsGatewayArgs } from "./CreateSmsGatewayArgs";
import { UpdateSmsGatewayArgs } from "./UpdateSmsGatewayArgs";
import { DeleteSmsGatewayArgs } from "./DeleteSmsGatewayArgs";
import { SmsLogFindManyArgs } from "../../smsLog/base/SmsLogFindManyArgs";
import { SmsLog } from "../../smsLog/base/SmsLog";
import { SmsGatewayService } from "../smsGateway.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SmsGateway)
export class SmsGatewayResolverBase {
  constructor(
    protected readonly service: SmsGatewayService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "read",
    possession: "any",
  })
  async _smsGatewaysMeta(
    @graphql.Args() args: SmsGatewayCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SmsGateway])
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "read",
    possession: "any",
  })
  async smsGateways(
    @graphql.Args() args: SmsGatewayFindManyArgs
  ): Promise<SmsGateway[]> {
    return this.service.smsGateways(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SmsGateway, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "read",
    possession: "own",
  })
  async smsGateway(
    @graphql.Args() args: SmsGatewayFindUniqueArgs
  ): Promise<SmsGateway | null> {
    const result = await this.service.smsGateway(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SmsGateway)
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "create",
    possession: "any",
  })
  async createSmsGateway(
    @graphql.Args() args: CreateSmsGatewayArgs
  ): Promise<SmsGateway> {
    return await this.service.createSmsGateway({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SmsGateway)
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "update",
    possession: "any",
  })
  async updateSmsGateway(
    @graphql.Args() args: UpdateSmsGatewayArgs
  ): Promise<SmsGateway | null> {
    try {
      return await this.service.updateSmsGateway({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => SmsGateway)
  @nestAccessControl.UseRoles({
    resource: "SmsGateway",
    action: "delete",
    possession: "any",
  })
  async deleteSmsGateway(
    @graphql.Args() args: DeleteSmsGatewayArgs
  ): Promise<SmsGateway | null> {
    try {
      return await this.service.deleteSmsGateway(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [SmsLog], { name: "logs" })
  @nestAccessControl.UseRoles({
    resource: "SmsLog",
    action: "read",
    possession: "any",
  })
  async findLogs(
    @graphql.Parent() parent: SmsGateway,
    @graphql.Args() args: SmsLogFindManyArgs
  ): Promise<SmsLog[]> {
    const results = await this.service.findLogs(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }
}
