/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { TicketCategory } from "./TicketCategory";
import { TicketCategoryCountArgs } from "./TicketCategoryCountArgs";
import { TicketCategoryFindManyArgs } from "./TicketCategoryFindManyArgs";
import { TicketCategoryFindUniqueArgs } from "./TicketCategoryFindUniqueArgs";
import { CreateTicketCategoryArgs } from "./CreateTicketCategoryArgs";
import { UpdateTicketCategoryArgs } from "./UpdateTicketCategoryArgs";
import { DeleteTicketCategoryArgs } from "./DeleteTicketCategoryArgs";
import { SupportTicketFindManyArgs } from "../../supportTicket/base/SupportTicketFindManyArgs";
import { SupportTicket } from "../../supportTicket/base/SupportTicket";
import { TicketCategoryService } from "../ticketCategory.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => TicketCategory)
export class TicketCategoryResolverBase {
  constructor(
    protected readonly service: TicketCategoryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "TicketCategory",
    action: "read",
    possession: "any",
  })
  async _ticketCategoriesMeta(
    @graphql.Args() args: TicketCategoryCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [TicketCategory])
  @nestAccessControl.UseRoles({
    resource: "TicketCategory",
    action: "read",
    possession: "any",
  })
  async ticketCategories(
    @graphql.Args() args: TicketCategoryFindManyArgs
  ): Promise<TicketCategory[]> {
    return this.service.ticketCategories(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => TicketCategory, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "TicketCategory",
    action: "read",
    possession: "own",
  })
  async ticketCategory(
    @graphql.Args() args: TicketCategoryFindUniqueArgs
  ): Promise<TicketCategory | null> {
    const result = await this.service.ticketCategory(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => TicketCategory)
  @nestAccessControl.UseRoles({
    resource: "TicketCategory",
    action: "create",
    possession: "any",
  })
  async createTicketCategory(
    @graphql.Args() args: CreateTicketCategoryArgs
  ): Promise<TicketCategory> {
    return await this.service.createTicketCategory({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => TicketCategory)
  @nestAccessControl.UseRoles({
    resource: "TicketCategory",
    action: "update",
    possession: "any",
  })
  async updateTicketCategory(
    @graphql.Args() args: UpdateTicketCategoryArgs
  ): Promise<TicketCategory | null> {
    try {
      return await this.service.updateTicketCategory({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => TicketCategory)
  @nestAccessControl.UseRoles({
    resource: "TicketCategory",
    action: "delete",
    possession: "any",
  })
  async deleteTicketCategory(
    @graphql.Args() args: DeleteTicketCategoryArgs
  ): Promise<TicketCategory | null> {
    try {
      return await this.service.deleteTicketCategory(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [SupportTicket], { name: "tickets" })
  @nestAccessControl.UseRoles({
    resource: "SupportTicket",
    action: "read",
    possession: "any",
  })
  async findTickets(
    @graphql.Parent() parent: TicketCategory,
    @graphql.Args() args: SupportTicketFindManyArgs
  ): Promise<SupportTicket[]> {
    const results = await this.service.findTickets(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }
}
