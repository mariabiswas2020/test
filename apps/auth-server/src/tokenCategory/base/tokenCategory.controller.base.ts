/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TokenCategoryService } from "../tokenCategory.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TokenCategoryCreateInput } from "./TokenCategoryCreateInput";
import { TokenCategory } from "./TokenCategory";
import { TokenCategoryFindManyArgs } from "./TokenCategoryFindManyArgs";
import { TokenCategoryWhereUniqueInput } from "./TokenCategoryWhereUniqueInput";
import { TokenCategoryUpdateInput } from "./TokenCategoryUpdateInput";
import { TokenFindManyArgs } from "../../token/base/TokenFindManyArgs";
import { Token } from "../../token/base/Token";
import { TokenWhereUniqueInput } from "../../token/base/TokenWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TokenCategoryControllerBase {
  constructor(
    protected readonly service: TokenCategoryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TokenCategory })
  @swagger.ApiBody({
    type: TokenCategoryCreateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createTokenCategory(
    @common.Body() data: TokenCategoryCreateInput
  ): Promise<TokenCategory> {
    return await this.service.createTokenCategory({
      data: data,
      select: {
        id: true,
        price: true,
        title: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TokenCategory] })
  @ApiNestedQuery(TokenCategoryFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async tokenCategories(
    @common.Req() request: Request
  ): Promise<TokenCategory[]> {
    const args = plainToClass(TokenCategoryFindManyArgs, request.query);
    return this.service.tokenCategories({
      ...args,
      select: {
        id: true,
        price: true,
        title: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TokenCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async tokenCategory(
    @common.Param() params: TokenCategoryWhereUniqueInput
  ): Promise<TokenCategory | null> {
    const result = await this.service.tokenCategory({
      where: params,
      select: {
        id: true,
        price: true,
        title: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TokenCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiBody({
    type: TokenCategoryUpdateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateTokenCategory(
    @common.Param() params: TokenCategoryWhereUniqueInput,
    @common.Body() data: TokenCategoryUpdateInput
  ): Promise<TokenCategory | null> {
    try {
      return await this.service.updateTokenCategory({
        where: params,
        data: data,
        select: {
          id: true,
          price: true,
          title: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TokenCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteTokenCategory(
    @common.Param() params: TokenCategoryWhereUniqueInput
  ): Promise<TokenCategory | null> {
    try {
      return await this.service.deleteTokenCategory({
        where: params,
        select: {
          id: true,
          price: true,
          title: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/tokens")
  @ApiNestedQuery(TokenFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Token",
    action: "read",
    possession: "any",
  })
  async findTokens(
    @common.Req() request: Request,
    @common.Param() params: TokenCategoryWhereUniqueInput
  ): Promise<Token[]> {
    const query = plainToClass(TokenFindManyArgs, request.query);
    const results = await this.service.findTokens(params.id, {
      ...query,
      select: {
        assignee: {
          select: {
            id: true,
          },
        },

        category: {
          select: {
            id: true,
          },
        },

        closedAt: true,
        createdAt: true,

        customer: {
          select: {
            id: true,
          },
        },

        description: true,
        id: true,
        status: true,
        ticketNo: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/tokens")
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "update",
    possession: "any",
  })
  async connectTokens(
    @common.Param() params: TokenCategoryWhereUniqueInput,
    @common.Body() body: TokenWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tokens: {
        connect: body,
      },
    };
    await this.service.updateTokenCategory({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/tokens")
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "update",
    possession: "any",
  })
  async updateTokens(
    @common.Param() params: TokenCategoryWhereUniqueInput,
    @common.Body() body: TokenWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tokens: {
        set: body,
      },
    };
    await this.service.updateTokenCategory({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/tokens")
  @nestAccessControl.UseRoles({
    resource: "TokenCategory",
    action: "update",
    possession: "any",
  })
  async disconnectTokens(
    @common.Param() params: TokenCategoryWhereUniqueInput,
    @common.Body() body: TokenWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tokens: {
        disconnect: body,
      },
    };
    await this.service.updateTokenCategory({
      where: params,
      data,
      select: { id: true },
    });
  }
}
