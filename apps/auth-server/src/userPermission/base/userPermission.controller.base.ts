/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { UserPermissionService } from "../userPermission.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { UserPermissionCreateInput } from "./UserPermissionCreateInput";
import { UserPermission } from "./UserPermission";
import { UserPermissionFindManyArgs } from "./UserPermissionFindManyArgs";
import { UserPermissionWhereUniqueInput } from "./UserPermissionWhereUniqueInput";
import { UserPermissionUpdateInput } from "./UserPermissionUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class UserPermissionControllerBase {
  constructor(
    protected readonly service: UserPermissionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: UserPermission })
  @swagger.ApiBody({
    type: UserPermissionCreateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createUserPermission(
    @common.Body() data: UserPermissionCreateInput
  ): Promise<UserPermission> {
    return await this.service.createUserPermission({
      data: {
        ...data,

        permission: {
          connect: data.permission,
        },

        user: {
          connect: data.user,
        },
      },
      select: {
        grantedAt: true,
        grantedBy: true,
        id: true,

        permission: {
          select: {
            id: true,
          },
        },

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [UserPermission] })
  @ApiNestedQuery(UserPermissionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async userPermissions(
    @common.Req() request: Request
  ): Promise<UserPermission[]> {
    const args = plainToClass(UserPermissionFindManyArgs, request.query);
    return this.service.userPermissions({
      ...args,
      select: {
        grantedAt: true,
        grantedBy: true,
        id: true,

        permission: {
          select: {
            id: true,
          },
        },

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: UserPermission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async userPermission(
    @common.Param() params: UserPermissionWhereUniqueInput
  ): Promise<UserPermission | null> {
    const result = await this.service.userPermission({
      where: params,
      select: {
        grantedAt: true,
        grantedBy: true,
        id: true,

        permission: {
          select: {
            id: true,
          },
        },

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: UserPermission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiBody({
    type: UserPermissionUpdateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateUserPermission(
    @common.Param() params: UserPermissionWhereUniqueInput,
    @common.Body() data: UserPermissionUpdateInput
  ): Promise<UserPermission | null> {
    try {
      return await this.service.updateUserPermission({
        where: params,
        data: {
          ...data,

          permission: {
            connect: data.permission,
          },

          user: {
            connect: data.user,
          },
        },
        select: {
          grantedAt: true,
          grantedBy: true,
          id: true,

          permission: {
            select: {
              id: true,
            },
          },

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: UserPermission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteUserPermission(
    @common.Param() params: UserPermissionWhereUniqueInput
  ): Promise<UserPermission | null> {
    try {
      return await this.service.deleteUserPermission({
        where: params,
        select: {
          grantedAt: true,
          grantedBy: true,
          id: true,

          permission: {
            select: {
              id: true,
            },
          },

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
