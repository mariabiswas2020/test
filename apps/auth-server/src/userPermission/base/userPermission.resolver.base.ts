/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { UserPermission } from "./UserPermission";
import { UserPermissionCountArgs } from "./UserPermissionCountArgs";
import { UserPermissionFindManyArgs } from "./UserPermissionFindManyArgs";
import { UserPermissionFindUniqueArgs } from "./UserPermissionFindUniqueArgs";
import { CreateUserPermissionArgs } from "./CreateUserPermissionArgs";
import { UpdateUserPermissionArgs } from "./UpdateUserPermissionArgs";
import { DeleteUserPermissionArgs } from "./DeleteUserPermissionArgs";
import { Permission } from "../../permission/base/Permission";
import { User } from "../../user/base/User";
import { UserPermissionService } from "../userPermission.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => UserPermission)
export class UserPermissionResolverBase {
  constructor(
    protected readonly service: UserPermissionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "read",
    possession: "any",
  })
  async _userPermissionsMeta(
    @graphql.Args() args: UserPermissionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [UserPermission])
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "read",
    possession: "any",
  })
  async userPermissions(
    @graphql.Args() args: UserPermissionFindManyArgs
  ): Promise<UserPermission[]> {
    return this.service.userPermissions(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => UserPermission, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "read",
    possession: "own",
  })
  async userPermission(
    @graphql.Args() args: UserPermissionFindUniqueArgs
  ): Promise<UserPermission | null> {
    const result = await this.service.userPermission(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => UserPermission)
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "create",
    possession: "any",
  })
  async createUserPermission(
    @graphql.Args() args: CreateUserPermissionArgs
  ): Promise<UserPermission> {
    return await this.service.createUserPermission({
      ...args,
      data: {
        ...args.data,

        permission: {
          connect: args.data.permission,
        },

        user: {
          connect: args.data.user,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => UserPermission)
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "update",
    possession: "any",
  })
  async updateUserPermission(
    @graphql.Args() args: UpdateUserPermissionArgs
  ): Promise<UserPermission | null> {
    try {
      return await this.service.updateUserPermission({
        ...args,
        data: {
          ...args.data,

          permission: {
            connect: args.data.permission,
          },

          user: {
            connect: args.data.user,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => UserPermission)
  @nestAccessControl.UseRoles({
    resource: "UserPermission",
    action: "delete",
    possession: "any",
  })
  async deleteUserPermission(
    @graphql.Args() args: DeleteUserPermissionArgs
  ): Promise<UserPermission | null> {
    try {
      return await this.service.deleteUserPermission(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Permission, {
    nullable: true,
    name: "permission",
  })
  @nestAccessControl.UseRoles({
    resource: "Permission",
    action: "read",
    possession: "any",
  })
  async getPermission(
    @graphql.Parent() parent: UserPermission
  ): Promise<Permission | null> {
    const result = await this.service.getPermission(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getUser(
    @graphql.Parent() parent: UserPermission
  ): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
